# PassSentry
PassSentry is a lightweight, privacy-first command-line tool that tells you whether a password has already been exposed in a known data breach. Built in Python and made by Ochena Gamer, it needs no installation beyond Python itself and has zero third-party dependencies, so you can run it on almost any machine in seconds.
